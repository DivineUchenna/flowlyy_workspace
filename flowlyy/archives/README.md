# archives

Completed projects, old workflows, closed conversations.

Nothing active lives here. When a client conversation ends (won or
lost), a workflow gets replaced, or a project wraps up, move it here
instead of deleting it — it stays searchable but out of the way of
day-to-day folders.
