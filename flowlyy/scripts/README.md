# scripts

Python and JavaScript scripts. Companies House API, lead generation,
data processing.

Anything that runs as code rather than as a Make/n8n scenario — one-off
data pulls, prospect list processing, API integrations that sit outside
`automations/`.
